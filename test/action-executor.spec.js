/*global sinon, beforeEach, describe, it, afterEach, expect, ko */
describe('ActionExecutor', function () {

    beforeEach(function () {
        return;
    });

    afterEach(function () {
        return;
    });

    it.skip('skipper', function () {
        expect(true, 'to be', true);
    });
});
